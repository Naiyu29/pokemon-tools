# Pokémon Champions 對戰工具 — 實作路線圖

目標：對戰中可用的「陣容推薦、傷害推估、速度線」，成本優先。
原則：**AI 只做建置與資料更新（一次性成本），對戰中使用零 token 成本（純網頁計算）。**

使用者背景：
- 手機 Android，主要在手機上使用
- 單打、雙打都玩
- 我的隊伍：`data/my-team.js`（隊名 Meta：仙子伊布/超壞星/熾焰咆哮虎/烈咬陸鯊/妙蛙花/噴火龍Y）
- 已完成：傷害計算管線（`src/analyze.js`，@smogon/calc + @pkmn/dex）、
  賽季報告（`out/report.html`，artifact 已發布）、截圖→Showdown paste 轉換（人工）

---

## M1｜對戰即用網頁工具（核心里程碑）

對戰中打開網頁 → 點選對手 6 隻 → 立即顯示三面板。全在瀏覽器端計算，零使用成本。

- [x] 1.1 計算核心改成瀏覽器可用（@smogon/calc 打包成 bundle，esbuild）→ `web/calc-core.js`＋`scripts/build.js`
- [x] 1.2 對手輸入 UI：中文名搜尋（支援注音頭字/英文），點 6 隻約 30–60 秒完成
      → 搜尋索引 `src/build-search-index.js`（全圖鑑＋形態，官方繁中名）
- [x] 1.3 威脅資料庫 `data/threats.js` 擴充到 Top 50，含常見配置（推估標註）；
      未知寶可夢 fallback 用極限值區間（耐久最少~最多）
      ※ 37 隻本季搜尋確認＋13 隻常青候補（rank 欄有標記）；新 Mega 特性用 megaAbility 覆寫 calc 佔位值
- [x] 1.4 三面板輸出：
      - 速度線（含圍巾/順風/天氣加成、同速標記）
      - 傷害矩陣（我方各招 vs 對面各隻，確1/亂1/確2 標記，可切單打/雙打、天氣）
      - 陣容推薦（規則評分：對面確1數、被確1數、速度優勢、特性剋制規則
        如金身擋變化招/不屈之心禁威嚇/沙暴搶天氣）
      ※ 已決定（2026-09-03）：採規則評分自動推薦選 4＋首發（Q1=A）
- [x] 1.5 部署 GitHub Pages（手機「加入主畫面」當 app 用），我的隊伍存 localStorage
      ※ 建置產物在 `docs/`＋PWA 離線；Pages 已設定完成（2026-09-05），
      網址 https://naiyu29.github.io/pokemon-tools/
- 驗收：選角 90 秒內完成輸入並看到建議；離線可用
- 成本：建置一次性；使用零成本

## M2｜資料維護流程

- [x] 2.1 建 skill（pokemon-champions）：記錄我的隊伍位置、截圖→paste 轉換規則、
      對點分析流程、威脅庫更新流程 → 新對話不用重講背景
      ※ 已決定（2026-09-03）：提前於 M1 之前建立（Q2=A）
- [x] 2.2 賽季更新 SOP：開新對話「更新 Champions 威脅資料庫」→ 搜尋新 meta → 重建 data → 重新部署
      ※ 完整 SOP 在 `skills-src/pokemon-champions/references/season-update.md`（含重建 bundle＋Pages 部署）；
      skill 有更新，記得同步到已安裝的 skill 庫
- [x] 2.3 戰績紀錄：先用網頁工具內建簡單勝負登錄（localStorage + 匯出 CSV），不做後端
      ※ 網頁新增「📓紀錄」分頁：勝/敗一鍵登錄（自動帶當前對手/模式/天氣＋備註）、
      勝率統計（總計/單打/雙打）、歷史列表可刪、匯出 CSV（UTF-8 BOM，Excel 可直開）
- [x] 2.4 我方隊伍多隊管理：隊伍庫（命名/切換/覆蓋/刪除，內建 Meta 不可刪；
      localStorage，舊單隊格式自動遷移）；戰績紀錄含使用隊伍、CSV 加「隊伍」欄
- 成本：每賽季一次短對話

## M3｜截圖半自動（賽前分析）

- [x] 3.1 流程化：對戰截圖貼給 Claude → 辨識對手 6 隻 → 回覆一條帶參數的工具連結
      （例 `?foes=primarina,tyranitar,...`），點開即完成預填
      ※ 網頁端補強：套用參數後清除 query（重載不蓋使用者編輯）、查無此名顯示
      「帶入失敗」提示、加 `weather` 參數；連結格式規則見
      `skills-src/pokemon-champions/references/battle-analysis.md`
- [x] 3.2 併入 M2 的 skill
      ※ SKILL.md 加工作流列＋工具網址；battle-analysis.md 加預填連結產生規則；
      skill 有更新，記得同步到已安裝的 skill 庫
- [x] 3.3 我方隊伍截圖辨識：隊伍狀態/能力頁截圖 → 辨識六隻 →
      回一條 `?team=隊名&mons=...` 連結，點開直接存進隊伍庫並切換
      ※ 工具端 `mons` 參數解析（id 還原正式名稱、EV、Mega 石自動掛型態、
        失敗顯示錯誤橫幅不吃掉）；規則見 `references/screenshot-to-paste.md`
- 定位：賽前 90 秒內來不及（一輪對話約 1–2 分鐘），適合賽後覆盤與遇到重複隊伍的預研
- 成本：每次一張圖＋短回覆（一輪對話）

## M4｜Android 即時辨識（選配、最後做）

- [x] 4.1 PWA Share Target：遊戲截圖 → 分享 → 直接進工具
      ※ manifest 加 `share_target`（POST multipart）、sw.js 收檔存快取後重導 `?shared=1`，
      自動開「截圖辨識」頁；需已安裝 PWA（加入主畫面）才會出現在分享選單
- [x] 4.2 端上辨識：sprite 樣板比對（不用 AI、零成本、毫秒級）——已用真實截圖驗證＋改版
      ※ 2026-09-05 用 8 張真實遊戲截圖重新調校：
      - 描述子庫改用 **smogon/sprites 的 `src/champions/` 遊戲內圖示**（跟截圖同款）優先、
        HOME 選單圖示補其餘、PokeAPI 渲染圖墊底；含 champions 官方**色違**同名列
        （實戰踩到色違快龍）。1601 筆 16×16（`data/sprite-index.bin` 約 1.2MB，離線快取）
      - **自動偵測對手 6 張卡**（右側深紅卡列 rgb≈135,5,50、綠色極低）＋自動全卡辨識：
        載圖即列每卡 top-3 候選（預選第 1），一鍵全部加入；點圖手動框選當備援
      - 抗干擾：前景遮罩門檻設上限＋形態學開運算去雷射細線＋連通元件丟煙霧/浮水印
      - 評分：512-bin 調色盤直方圖交集(0.65)＋4×4 區塊色(0.35)——不受對位/縮放影響
      - **Champions 登場名單加權**（+0.08，撞色路人降權）＋卡框以中位數對齊
        （修舞台燈光拉歪的卡）＋瀏覽器端不縮圖（保住小隻 sprite）
      - 描述子 16×16→**24×24**（bin 約 2.8MB；32px 不再更準只是更肥）＋
        選角候選**排除 Mega**（進場後才會 Mega）——45 卡調校集 **top1 100%**
        （在同一組截圖上調出來的數字，新截圖預期略低；集子要持續擴充）。
        `scripts/test-recognizer.js` 合成測試因亮度擾動偏嚴，僅當回歸參考
      - UI：每卡 top-5 候選（預選第 1）＋🔍 搜尋自選；「取代對手」整批換掉
        對手清單（每次辨識＝一場新對戰），完成後直接跳推薦頁；
        紀錄分頁登錄勝敗時自動帶這批對手
- [ ] 4.3 懸浮視窗 overlay——**評估後擱置（2026-09-05）**：PWA 做不到 overlay，需原生
      app（Android SYSTEM_ALERT_WINDOW）＋螢幕擷取權限，工程量大；分享截圖（4.1）＋
      分割畫面已覆蓋大部分需求，等實際使用發現不夠再議

---

## 開新對話的時機（省上下文成本）

- **每個里程碑開一個新對話**，開頭說：「讀 pokemon-tools repo 的 ROADMAP.md，做 M1」
- 狀態全部存 repo（隊伍、威脅庫、進度勾選），對話本身不承載狀態
- 本串（研究＋雛形）到此收尾；問答、對戰截圖分析等輕量需求可另開短對話
- 完成一項就把本檔的 checkbox 打勾並 commit
